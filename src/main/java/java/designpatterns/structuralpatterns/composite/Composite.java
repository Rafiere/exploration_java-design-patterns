package java.designpatterns.structuralpatterns.composite;

public class Composite {

	/* Esse padrão só faz sentido quando o modelo "core" da aplicação pode
	* ser representado como uma árvore. */

	/* Basicamente, esse padrão realiza a composição de objetos em estruturas
	* de árvores e então trabalha com essas estruturas como se elas fossem
	* objetos individuais. */
}
