package java.designpatterns.structuralpatterns.decorator.eedecoratorcompleto;

public class DecoratorCompleto {

	/* Nesse exemplo, será apresentado um decorator completo. */

	/* Agora, temos uma classe abstrata para armazenarmos a parte que é comum dos
	* decorators, que, nesse caso, é o método construtor. */
}
