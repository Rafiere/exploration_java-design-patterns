package java.designpatterns.structuralpatterns.adapter;

public class Adapter {

	/* O padrão "Adapter" permite a colaboração entre objetos que possuem
	* interfaces compatíveis. */

	/* O padrão adapter possui o "client", o "adapter" e o "adaptee". */

	/* O "Client" é quem está usando o que foi implementado. */
}
