package java.designpatterns.structuralpatterns.bridge.bbdevicesimplementation;

public class DevicesImplementation {

	/* Nesse exemplo, temos a separação entre as classes dos dispositivos e
	* os dispositivos remotos que eles controlam. */

	/* Os remotos são as abstrações, e os dispositivos são as implementações. */

	/* Por causa das interfaces em comum, ,os mesmos remotos podem funcionar com
	* diferentes dispositivos, e vice-versa. */
}
