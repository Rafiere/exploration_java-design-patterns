package java.designpatterns.structuralpatterns.flyweight.ccresolucaodoproblema;

public class ResolucaoDoProblema {

	/* Nesse exemplo, tentaremos diminuir o custo de memória do exemplo "BB" aplicando o
	* padrão "flyweight". */

	/* Basicamente, como, dos 5 atributos da classe "Book", apenas os atributos
	* "name" e "price" mudam com frequência, moveremos os atributos
	* "type", "distributor" e "otherData" para um novo objeto. */
}
