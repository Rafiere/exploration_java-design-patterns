package java.designpatterns.structuralpatterns.flyweight;

public class Flyweight {

	/* Esse é um padrão de projeto com um foco muito grande na economia de memória. */

	/* Um caractere possui 8 bits. Quando transformamos ele em uma classe, ele passará
	* a ter, no mínimo, 125 bytes. Isso significa, no mínimo, que multiplicamos o tamanho
	* dele por 125. */

	/* Esse padrão foi usado para impedir que o editor use gigabytes de memória apenas
	* para abrir um livro. */

	/* Esse é um padrão para otimização. Só devemos utilizar esse padrão quando
	* necessário. */
}
