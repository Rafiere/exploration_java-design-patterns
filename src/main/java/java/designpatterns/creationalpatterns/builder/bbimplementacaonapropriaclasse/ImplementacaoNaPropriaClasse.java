package java.designpatterns.creationalpatterns.builder.bbimplementacaonapropriaclasse;

public class ImplementacaoNaPropriaClasse {

	/* Esse é o padrão mais simples. Declaramos os métodos que
	* compõem o padrão "Builder" dentro da própria classe que
	* será construída pelo builder. */

	/* Com esse padrão, não precisamos nem chamar o método
	* "build()" após construir o objeto. */

	/* Ele pode ser usado quando temos objetos que recebem
	* muitas propriedades para serem construídos. */
}
