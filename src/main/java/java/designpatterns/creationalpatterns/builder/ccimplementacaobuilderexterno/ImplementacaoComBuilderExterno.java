package java.designpatterns.creationalpatterns.builder.ccimplementacaobuilderexterno;

public class ImplementacaoComBuilderExterno {

	/* Nesse exemplo, utilizaremos um builder em outra classe. É recomendado que a classe builder esteja
	* no mesmo pacote do modelo. Sempre é adotado o padrão "Address" e "AddressBuilder", por exemplo. */
}
