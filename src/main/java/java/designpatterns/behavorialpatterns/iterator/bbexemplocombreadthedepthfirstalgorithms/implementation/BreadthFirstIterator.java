package java.designpatterns.behavorialpatterns.iterator.bbexemplocombreadthedepthfirstalgorithms.implementation;

/* Essa é a outra implementação do algoritmo em questão. */
public class BreadthFirstIterator<T> implements Iterator<T> {

	@Override
	public boolean hasNext() {

		/* Aqui, temos a implementação do "hasNext()" dessa estrutura. */

		return false;
	}

	@Override
	public Vertex<T> getNext() {

		/* Aqui, temos a implementação do "getNext()" nessa estrutura. */

		return null;
	}

	@Override
	public void reset() {

		/* Aqui, temos a implementação do método "reset()" nessa estrutura. */
	}
}
