package java.designpatterns.behavorialpatterns.iterator.aaconceitos;

public class Conceitos {

	/* Se temos um grafo e queremos iterar sobre os elementos desse grafo, mas sem
	* expor a implementação dele, ou seja, ele pode ser uma binary search tree, uma red black
	* search tree e etc. */

	/* O principal objetivo desse padrão é extrair a implementação de uma coleção em um
	* objeto separado, que é chamado de iterator. */

	/* Muitos iterators podem estar na mesma coleção ao mesmo tempo. */

	/* Todos os iterators devem implementar a mesma interface */
}
