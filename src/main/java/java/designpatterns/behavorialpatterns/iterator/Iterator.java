package java.designpatterns.behavorialpatterns.iterator;

public class Iterator {

	/* Esse padrão serve para encapsularmos uma determinada estrutura de dados e
	* utilizarmos ela sem expor a implementação. */

	/* Novos algoritmos podem ser utilizados, pois estamos dependendo da
	* interface, e não da implementação. */
}
