package java.designpatterns.behavorialpatterns.memento;

public class Memento {

	/* Esse é um padrão que permite que salvemos e restauremos o estado de um objeto sem
	* revelar os detalhes de sua implementação. */

	/* É criada uma snapshot do estado atual do objeto. */

	/* A classe original pode fazer as snapshots dela mesma. */
}
