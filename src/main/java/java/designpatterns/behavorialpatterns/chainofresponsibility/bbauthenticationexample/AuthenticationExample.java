package java.designpatterns.behavorialpatterns.chainofresponsibility.bbauthenticationexample;

public class AuthenticationExample {

	/* Nesse exemplo, faremos um sistema de autenticação. Esse sistema validará o nome do
	* usuário, a senha e verificará se o usuário que está tentando logar é um administrador. */
}
