package java.designpatterns.behavorialpatterns.visitor;

public class Visitor {

	/* O padrão de projeto "Visitor" permite a separação do comportamento de um
	* algoritmo dos objetos em que eles operam. */

	/* Basicamente, colocamos todos esses algoritmos em uma única classe. */

	/* Com isso, também aplicamos o "Open/Closed Principle", pois novos comportamentos
	* de "Visitor" podem ser facilmente introduzidos sem que comportamentos já
	* existentes sejam modificados. */
}
