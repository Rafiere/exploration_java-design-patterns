package java.designpatterns.behavorialpatterns.command.bbabordagemoop;

public class AbordagemOOP {

	/* Nesse cenário, temos o "Command", o "Receiver", o "Invoker" e o "Client". */

	/* Esse exemplo criará uma aplicação para performar algumas operações de
	* texto, como abrir, escrever e salvar um texto. A aplicação será quebrada nos
	* quatro componentes acima. */

	/* Um "command" é o objeto que vai armazenar toda a informação requerida para
	* executar uma ação, incluindo o método para chamar, os argumentos do método e o
	* objeto (que é conhecido como "receiver"), que implementa o método. */
}
