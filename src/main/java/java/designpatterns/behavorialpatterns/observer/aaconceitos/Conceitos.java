package java.designpatterns.behavorialpatterns.observer.aaconceitos;

public class Conceitos {

	/* Esse padrão permite que objetos sejam notificados quando algo ocorra. Ele é usado quando
	* a mudança no estado de um objeto requer a mudança em outros objetos. */

	/* Isso é muito encontrado em GUIs, assim, quando um determinado clique ocorrer em uma
	* determinada área, um evento será disparado e todos aqueles que estão observando esse
	* evento, serão notificados. */

	/* Ele deve ser utilizado quando os inscritos podem se juntar ou sair da lista sempre
	* que eles acharem que devem. */
}
