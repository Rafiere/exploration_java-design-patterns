package java.designpatterns.behavorialpatterns.observer;

public class Observer {

	/* Esse é um design pattern do tipo "behavioral". */

	/* Ele notifica múltiplos objetos (subscribers) sobre quaisquer eventos que aconteça com
	 * o objeto que eles estão observando, ou "publisher". */

	/* Basicamente, esse padrão permite mudar ou realizar uma ação em um conjunto de objetos quando o
	* estado de outro objeto mudar. Estamos aplicando o "Open/Closed" principle, dessa forma, podemos
	* introduzir novas classes "subscriber" sem ter que mudar o código de quem está publicando. */
}
